# custom_heap.cpp
Custom C++ Heap Implementation
A self-implemented Max Heap data structure in C++ supporting efficient priority queue operations such as insertion, deletion, and retrieval of the maximum element in O(log n) time. Built using vector-based array representation and heapify-up / heapify-down mechanisms.
